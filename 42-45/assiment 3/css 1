:root {
  --mainColor: #009688;
  --mainPadding: 10px;
}

div {
  color: var(--mainColor, black);
  padding: calc(10px + var(--mainPadding));
  border:  4px solid   var(--mainColor);
  margin: 10px;
  font-weight: 800;
}
